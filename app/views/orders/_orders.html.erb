 <table class="table">
    <td><%= order.product_name%></td>
    <td> <%= order.product_count %></td>
    <td><%= link_to 'Edit', edit_order_path(order), class: "btn btn-warning" %></td>
    <td><%=link_to'Delete', order, method: :delete, class: "btn btn-danger" %></td>

  </table>